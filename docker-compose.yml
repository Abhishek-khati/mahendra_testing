services:
  db:
    image: mysql:8.4
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: chainshield
      MYSQL_USER: chainshield
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?set MYSQL_PASSWORD in .env}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?set MYSQL_ROOT_PASSWORD in .env}
    ports:
      - "3306:3306"
    volumes:
      - chainshield-db:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "chainshield", "-p${MYSQL_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10
  app:
    build: .
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: mysql://chainshield:${MYSQL_PASSWORD}@db:3306/chainshield
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in .env}
      # Configure CHAINSHIELD_WORKER_URL and CHAINSHIELD_WORKER_TOKEN only when a separate sandbox is deployed.
      CHAINSHIELD_WORKER_URL: ${CHAINSHIELD_WORKER_URL:-}
      CHAINSHIELD_WORKER_TOKEN: ${CHAINSHIELD_WORKER_TOKEN:-}
    ports:
      - "3000:3000"
volumes:
  chainshield-db:
